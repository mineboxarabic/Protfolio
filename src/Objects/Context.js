import { createContext, useContext } from "react";

export const isMouseOverAComboBox = createContext(false);

export const colorPalette = {
    green: '#00d646',
    darkGrey: '#2f2e2e',
    darkBlue: '#333b50',
    white: '#fff',
    lightGrey: '#BEC7D5',  
}
